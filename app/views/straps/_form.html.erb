<%= form_with(model: strap, local: true) do |form| %>
  <% if strap.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(strap.errors.count, "error") %> prohibited this strap from being saved:</h2>

      <ul>
      <% strap.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :guitar_id %>
    <%= form.text_field :guitar_id %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :size %>
    <%= form.text_field :size %>
  </div>

  <div class="field">
    <%= form.label :in_stock_quantity %>
    <%= form.number_field :in_stock_quantity %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
